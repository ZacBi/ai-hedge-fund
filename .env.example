# =============================================================================
# 简化配置：若仅使用 OpenRouter 作为 LLM 入口，只需配置以下两项即可使用多数模型
# （OpenRouter 支持 OpenAI / Anthropic / Google / DeepSeek / xAI 等，一个 key 即可）
# =============================================================================
# FINANCIAL_DATASETS_API_KEY=your-financial-datasets-api-key  # 财经数据，必配
# OPENROUTER_API_KEY=your-openrouter-api-key                  # 从 https://openrouter.ai/ 获取

# For getting financial data to power the hedge fund
# Get your Financial Datasets API key from https://financialdatasets.ai/
FINANCIAL_DATASETS_API_KEY=your-financial-datasets-api-key

# For running LLMs hosted by openai (GPT 5, etc.)
# Get your OpenAI API key from https://platform.openai.com/
OPENAI_API_KEY=your-openai-api-key

# For running LLMs hosted by anthropic (claude-4-sonnet, claude-4.1-opus, etc.)
# Get your Anthropic API key from https://anthropic.com/
ANTHROPIC_API_KEY=your-anthropic-api-key

# For running LLMs hosted by deepseek (deepseek-chat, deepseek-reasoner, etc.)
# Get your DeepSeek API key from https://deepseek.com/
DEEPSEEK_API_KEY=your-deepseek-api-key

# For running LLMs hosted by groq (deepseek, llama3, etc.)
# Get your Groq API key from https://groq.com/
GROQ_API_KEY=your-groq-api-key

# For running LLMs hosted by gemini (gemini-2.5-flash, gemini-2.5-pro)
# Get your Google API key from https://ai.dev/
GOOGLE_API_KEY=your-google-api-key

# For running LLMs hosted by xAI (Grok 4, etc.)
XAI_API_KEY=your-xai-api-key

# For running LLM GigaChat
GIGACHAT_API_KEY=your-gigachat-api-key

# For running LLMs hosted by OpenRouter (推荐：一个 key 可调用 GPT/Claude/Gemini/DeepSeek/xAI 等)
# 在应用中选择 provider 为 OpenRouter 的模型即可，无需配置各厂商独立 API key
# Get your key from https://openrouter.ai/
OPENROUTER_API_KEY=your-openrouter-api-key

# For running LLMs hosted by Microsoft Azure OpenAI (gpt-4o, gpt-4o-mini, etc.)
# Get your Azure OpenAI API settings from https://oai.azure.com/
AZURE_OPENAI_API_KEY=your-azure-openai-api-key
AZURE_OPENAI_ENDPOINT=your-azure-openai-endpoint
AZURE_OPENAI_DEPLOYMENT_NAME=your-azure-openai-deployment-name

# -----------------------------------------------------------------------------
# OpenAI 兼容 API（任意自建或第三方兼容 OpenAI 格式的 endpoint）
# 配置 BASE URL 与 API Key 后，在应用中选择 provider 为 "OpenAI Compatible" 的模型即可
# -----------------------------------------------------------------------------
OPENAI_COMPATIBLE_API_KEY=your-openai-compatible-api-key
OPENAI_COMPATIBLE_BASE_URL=https://your-compatible-api.com

# -----------------------------------------------------------------------------
# 阿里云百炼（通义千问等，OpenAI 兼容模式）
# API Key 从 https://bailian.console.aliyun.com/ 密钥管理 获取
# 地域：中国内地默认 dashscope.aliyuncs.com；美国 dashscope-us；新加坡 dashscope-intl
# -----------------------------------------------------------------------------
DASHSCOPE_API_KEY=your-dashscope-api-key
# DASHSCOPE_BASE_URL=https://dashscope.aliyuncs.com/compatible-mode/v1

# -----------------------------------------------------------------------------
# Langfuse 可观测（LLM 调用追踪、延迟与成本）+ Prompt 管理
# 注册 https://cloud.langfuse.com 或自建后填写；不配置则不影响运行
# 同一套 key 用于 tracing 与 Prompt 管理。若要从 Langfuse 拉取 prompt，需先运行：
#   uv run scripts/sync_prompts_to_langfuse.py
# 在 Langfuse 中创建对应 name 的 chat prompt 后，运行时将优先使用 Langfuse 版本。
# -----------------------------------------------------------------------------
# LANGFUSE_PUBLIC_KEY=pk-lf-...
# LANGFUSE_SECRET_KEY=sk-lf-...
# LANGFUSE_BASE_URL=https://cloud.langfuse.com

# -----------------------------------------------------------------------------
# LangSmith 可观测（LangChain 官方，LLM 调用追踪）
# 注册 https://smith.langchain.com 后填写；不配置则不影响运行
# -----------------------------------------------------------------------------
# LANGSMITH_TRACING=true
# LANGSMITH_API_KEY=your-langsmith-api-key
# LANGSMITH_PROJECT=ai-hedge-fund